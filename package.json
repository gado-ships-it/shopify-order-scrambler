{
  "name": "shopify-order-scrambler",
  "version": "1.0.0",
  "description": "Shopify app that randomizes customer-facing order IDs to prevent volume inference",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.6.0",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
